# P2.3 — Advanced Kiosk

## Implemented
- Customer-facing kiosk route: `/kiosk/[kioskCode]`
- Public rate-limited kiosk menu/config API
- Kiosk registration/admin management
- Kiosk heartbeat / last-seen tracking
- Self-ordering cart
- Modifiers
- Combo menu support using existing combo menu schema
- Upsell recommendations using `p2_3_kiosk_upsells`
- Server-authoritative pricing/tax calculation
- Idempotent kiosk order creation
- Existing `orders` / `order_items` reuse
- KDS ticket creation
- Token generation using existing `order_tokens`
- Print queue creation using existing `print_jobs`
- UPI manual QR flow
- Cashfree online flow when configured
- Cash fallback
- Local menu cache
- Offline cash-order queue with retry/sync
- Restaurant isolation and public rate limiting
- Existing POS code preserved

## Verification
- New JS route files pass `node --check`.
- Supabase P2.3 functions verified as SECURITY INVOKER.
- New final order RPC execution revoked from `public`, `anon`, and `authenticated`; granted to `service_role`.
- P2.3 RLS-enabled tables verified in Supabase.
- Supabase security advisor shows no P2.3-specific SECURITY DEFINER finding. Existing unrelated project-wide findings remain.

## Remaining certification gate
A dependency-installed production `next build` and live browser/payment/printer/device E2E run could not be completed in this environment because the working ZIP has no installed `node_modules`, and `npm install --ignore-scripts --no-audit --no-fund` timed out.

Therefore this artifact is **implementation-complete but not honestly marked production-certified** until the build and runtime E2E gate passes.
